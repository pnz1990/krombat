apiVersion: apps/v1
kind: Deployment
metadata:
  name: rpg-frontend
  namespace: rpg-system
spec:
  replicas: 4
  selector:
    matchLabels:
      app: rpg-frontend
  template:
    metadata:
      labels:
        app: rpg-frontend
    spec:
      topologySpreadConstraints:
        # #471: 4 replicas, maxSkew:1 across 2 AZs — steady state is [2,2]
        # maxSkew:0 causes pending pods during rolling update (scheduler sees 3 zones)
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app: rpg-frontend
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: frontend
          # #415: nginxinc/nginx-unprivileged runs as UID 101 (non-root).
          # Listens on port 8080 instead of privileged 80.
          image: 319279230668.dkr.ecr.us-west-2.amazonaws.com/krombat/frontend:91330b0eed1b61c7de0da18ce26a8ffb0ad72c38
          ports:
            - containerPort: 3000
          securityContext:
            runAsNonRoot: true
            runAsUser: 101
            runAsGroup: 101
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: nginx-cache
              mountPath: /var/cache/nginx
            - name: nginx-run
              mountPath: /var/run
            - name: nginx-tmp
              mountPath: /tmp
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
      volumes:
        - name: nginx-cache
          emptyDir: {}
        - name: nginx-run
          emptyDir: {}
        - name: nginx-tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: rpg-frontend
  namespace: rpg-system
spec:
  selector:
    app: rpg-frontend
  ports:
    - port: 3000
      targetPort: 3000
